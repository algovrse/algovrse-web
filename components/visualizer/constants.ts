export enum VisualizerActions {
  Swap = 'swap',
  Compare = 'compare',
  Done = 'done',
  Select = 'select',
  None = 'none',
}

export enum VisualizerStatus {
  IDLE = 'IDLE',
  RUNNING = 'RUNNING',
  FINISHED = 'FINISHED',
  PAUSED = 'PAUSED',
  STOPPED = 'STOPPED',
}
